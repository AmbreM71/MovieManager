#ifndef ENUMS_H
#define ENUMS_H

enum Theme {
    Classic,
    Dark,
    OLED,
    MidnightPurple,
};

enum Language {
    French,
    English,
};

enum LogCriticity {
    Notice,
    Warning,
    Error,
};

#endif // ENUMS_H
